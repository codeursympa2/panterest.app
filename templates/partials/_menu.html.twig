<nav class="navbar navbar-expand-sm navbar-light bg-light text-dark mb-4" aria-label="Third navbar example">
		<div class="container-fluid">
        <div class="container">
			<a class="navbar-brand mt-5" href=" {{ path('home') }} " style="margin-top:50px;">Panterest</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample03" aria-controls="navbarsExample03" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarsExample03">
				<ul class="navbar-nav ml-auto" style="margin-left:auto;">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link  " aria-current="page" href=" {{ path('create_pin') }} ">New Pin</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('account') }}">Account</a>
					</li>

					<li class="nav-item">
						<!-- Avec GET -->
						<a class="nav-link" onclick=" return confirm('Voulez-vous vraiement vous déconnectez?');" href="{{ logout_path('main') }}">Logout</a>
						<!-- Avec POST -->
						<!-- <a class="nav-link" onclick="event.preventDefault();confirm('Voulez-vous vraiement vous déconnectez?') && document.getElementById('form-logout').submit();" href="#">Logout</a>-->
					</li>
					<!-- Suite POST
					<form method="POST" id="form-logout" action="{{ path('app_logout') }}" style="display:none;">
						<input type="hidden" name="csrf_token" value="{{ csrf_token('logout') }}">
					</form>
					 -->
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path ('app_register') }}">Register</a>
					</li>
					<li class="nav-item">
						<a class="nav-link " href="{{ path ('app_login') }}">Login</a>
					</li>
				{% endif %}
				</ul>

			</div>
		</div>
	
</nav>
